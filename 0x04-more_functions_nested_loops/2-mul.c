#include "main.h"

/**
 * mul - function that multiplies two integer
 * @a: first character
 * @b: second character
 * Return: the product
 */
int mul(int a, int b)
{
	int product = a * b;

	return (product);
}
